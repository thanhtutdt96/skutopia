export enum ErrorMessage {
  BAD_REQUEST = 'Invalid request',
  SERVER_ERROR = 'Internal server error',
  ORDER_NOT_FOUND = 'Sales order not found',
  ORDER_BOOKED = 'Sales order is already booked',
}
